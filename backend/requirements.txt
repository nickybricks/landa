sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
openai>=1.0.0
flask>=3.0.0
# pywhispercpp powers the bundled landa-base model via whisper.cpp + Metal
pywhispercpp>=1.2.0
# faster-whisper powers the user-downloadable local Whisper models (cross-platform, CTranslate2)
faster-whisper>=1.0.0
# huggingface_hub is required for model downloads (auto-installed with faster-whisper)
huggingface_hub>=0.20.0
# anthropic is optional — only required when llm_provider == "anthropic"
anthropic>=0.26.0
# websocket-client is required for realtime streaming transcription (gpt-4o-transcribe models)
websocket-client>=1.6.0
